# TODO

## 🔴 En cours

## 🟡 À faire
- [ ] FIX: l'erreur d'hydratation React (id radix-_R_… mismatch) est un artefact dev-only de Next 16.1.5 (boundary interne en tête de `<body>` côté SSR) — absente en build de production. **Upgrade fait (2026-08-08, Next 16.3.0), re-test toujours non fait.** Vérifié le 2026-08-15 : le conteneur `next` sert bien **16.3.0** (version grepée dans le bundle client servi par `localhost:3020`), donc le `make reset` qu'on croyait nécessaire ne l'est pas — le volume `next_node_modules` est à jour. Ne manque plus que l'observation : une erreur d'hydratation ne se voit que dans la console du navigateur, et l'agent n'a pas de navigateur utilisable ici (Chromium refusé par le bac à sable, `bootstrap_check_in … Permission denied`). **À faire côté humain** : ouvrir http://localhost:3020 et regarder la console.
- [ ] CHORE: 10+ branches `dependabot/bun/apps/next/*` ouvertes sur le remote (react 19.2.7, radix-*, @hey-api/openapi-ts 0.98.2, eslint 10.x alors qu'eslint a été retiré au profit d'oxlint). À trier/fermer une fois la branche mergée, plutôt que de rebaser 10 PR sur un diff de 222 fichiers. (Non fait : la branche n'est pas mergée — la condition posée par la tâche elle-même — et l'API GitHub est injoignable depuis le bac à sable, `tls: failed to verify certificate`. Fermer des PR reste de toute façon une action sortante à décider par toi.)

## 🟢 Idées / backlog

## 🤖 Claude — recommandations

- [ ] TEST: `apps/server/api/q_a.py` (72.41 %) et `api/agent.py` (75.86 %) n'ont plus que les branches non couvertes traitées ce coup-ci ailleurs (`except … -> 503 / 404 / 500`). Les deux tables paramétrées ajoutées à `tests/api/test_dataset.py` se transposent telles quelles — gain rapide.
- [ ] CHORE: `apps/next/tsconfig.typecheck.json` recopie `include`/`exclude` du tsconfig principal pour en retirer `.next` ; TS n'a pas d'héritage partiel sur ces clés, donc un changement d'`include` doit être répercuté dans les deux fichiers.
- [ ] TEST: le nouveau limiteur Redis n'est testé que contre un faux backend — le chemin réel (`RateLimitBackend` + vrai Redis, donc le compteur Lua atomique) n'est vérifié nulle part. Les tests tournent déjà avec testcontainers pour Postgres : un conteneur `redis:7-alpine` fermerait le trou, y compris sur le point qui a motivé la migration (deux `hit` concurrents ne doivent pas dépasser le plafond).
- [ ] DOCS: `AUTH_LOGIN_MAX_ATTEMPTS`, `AUTH_LOGIN_WINDOW_SECONDS`, `REDIS_URL` et les réglages du SDK (`REDIS_PREFIX`, `REDIS_RATE_LIMIT_FAIL_CLOSED`) ne sont documentés que dans les commentaires de `core/config.py` / `services/rate_limit.py` — absents du README et de `.env.example`, alors que le README a déjà un tableau d'env pour le crawl.
- [ ] CHORE: bump de l'image Redis (`redis:7-alpine` → 8.x) : le SDK utilise `INCREX` (Redis ≥ 8.8) quand il est là et retombe sinon sur un script Lua — les deux sont atomiques, mais `INCREX` économise le chargement du script et est le chemin nominal du SDK.
- [ ] TEST: `TestClient.stream()` se bloque indéfiniment sur un générateur SSE sans fin — le pont synchrone attend un corps qui ne se termine jamais. C'est pourquoi `tests/api/test_debug.py` pilote `response.body_iterator` directement. À savoir avant d'écrire le test du prochain endpoint SSE.
- [ ] CHORE: décider du sort de `apps/next/AGENTS.md` et `apps/next/CLAUDE.md` — Next 16.3 les (re)génère à chaque `next dev` (`node_modules/next/dist/server/lib/generate-agent-files.js`) ; soit on les commite, soit on les `.gitignore`, sinon le tree est sale en permanence.
- [ ] FIX: `api-check` compare avec `git diff --exit-code` (donc vs l'index) — un client régénéré mais seulement `git add`é passe au vert en local alors que la CI, elle, échouerait. Utiliser `git diff HEAD --exit-code` pour que local et CI disent la même chose.
- [ ] FEAT: rendre le seeding de dev auto-réparateur au lieu d'idempotent-silencieux — aujourd'hui un compte existant est sauté, donc une ligne périmée (mot de passe d'un ancien `DEV_ADMIN_PASSWORD`) empoisonne le dev sans trace. En développement, réaligner le hash sur le mot de passe attendu plutôt que de sauter.
- [ ] REFACTOR: le seeding est derrière deux verrous (`ENVIRONMENT=development` **et** `SEED_DEV_USERS=true`) ; oublier l'un des deux donne zéro utilisateur et un simple 401. Le déclencher par défaut dès `is_development`, en gardant `SEED_DEV_USERS` comme override explicite hors dev.
- [ ] FIX: chemin relatif au CWD — `script_location = server/migrations` (alembic) ne résout correctement que depuis `apps/`. Lancer l'app depuis la racine du repo échoue avec `Path doesn't exist: server/migrations`. L'ancrer sur un chemin absolu dérivé du package. (Le volet `sqlite:///./datasets.db` est réglé : la base est passée sur PostgreSQL.)
- [ ] CHORE: supprimer les 3 `datasets.db` résiduels sur l'hôte (`./`, `./apps/`, `./apps/server/`) — vestiges de SQLite, plus lus par rien depuis le passage à PostgreSQL. Non tracés par git, donc simple `rm`.
- [ ] DOCS: `.env.example` ne contient pas `ENVIRONMENT` alors que `make env` en fait le `.env` de départ. Sans lui l'app retombe sur `production` : le seeding refuse les mots de passe par défaut et le CORS se limite à `frontend_url` (pas de wildcard localhost). Ajouter `ENVIRONMENT=development` juste au-dessus de `SEED_DEV_USERS=true`.
- [ ] FIX: la suppression (déjà indexée) de `conftest.py` à la racine casse la collecte pytest dans le bac à sable de l'agent — `PermissionError` sur `.env`, avant même le premier test. C'est précisément ce que son `collect_ignore_glob` évitait ; la CI n'est pas touchée (pas de `.env` sur un checkout neuf). Soit le restaurer, soit déplacer l'exclusion dans `pytest.ini` (`norecursedirs`/`--ignore`).
- [ ] CHORE: le mockup statique `/dataset-detail` fait doublon avec la vraie page `/datasets/[id]` désormais portée, mais il héberge le CSS `.dd-page` que celle-ci importe. Si le mockup part, le CSS doit partir avec la page, pas avec lui.
- [ ] FEAT: la page globale `/sources` groupe encore par `metadata.source_url` du *dataset* (donc la seule graine du dernier run) : un crawl de 50 pages y compte pour une source. Elle peut maintenant s'appuyer sur `GET /dataset/{name}/sources`, comme `useAllDatasetRuns` le fait pour les runs.
- [ ] REFACTOR: l'onglet « Q/A pairs » du détail rend encore `QAList`/`QAItem` en Tailwind coloré (bleu/vert/jaune) à l'intérieur du shell `.card` : le reste de la page suit le design system. Porter ces deux composants sur `.qa`/`.qa-meta` (déjà stylés dans `dataset-detail.css`) fermerait l'écart.
- [ ] CHORE: un `next dev` lancé hors Docker écrase le `.next` du conteneur (bind-mount `./apps/next:/app`) et met les deux serveurs en boucle de redémarrage. Documenter le point, ou donner un `distDir` distinct au dev local.

## ✅ Fait

- [x] 2026-08-15 — FEAT: le limiteur anti-brute-force du login passe sur le SDK officiel `fastapi-redis-sdk` (paquet `redis_fastapi`), arbitrage rendu par toi ce jour — j'avais signalé la réserve (dépendance alpha 0.8.0 devant un contrôle d'authentification), tu as tranché pour l'adoption. `RedisSlidingWindowRateLimiter` (sorted set maison) devient `RedisRateLimiter`, façade au-dessus de `RateLimitBackend`. Le vrai gain est **l'atomicité** : l'ancien backend faisait `zremrangebyscore` + `zcard` + `zadd` en aller-retours séparés, donc deux tentatives simultanées pouvaient toutes deux se voir sous le plafond et passer ; le SDK compte via `INCREX` (Redis ≥ 8.8) ou, à défaut, un script Lua atomique — et si le serveur n'a ni l'un ni l'autre il remonte une erreur plutôt que de compter faux (l'image du compose est `redis:7-alpine`, donc c'est la voie Lua). Contrepartie assumée : fenêtre glissante → fenêtre fixe côté Redis (le backend en mémoire reste glissant) ; pour un throttle anti-brute-force c'est un effet de bord, pas un trou, le plafond par fenêtre est inchangé. Deux adaptations restent chez nous. La sémantique « seuls les échecs comptent » : `register_failure` appelle `hit`, tandis que `retry_after` lit avec `peek`, qui ne consomme pas — un login légitime ne dépense donc jamais de quota (testé). Et la garantie « rien ne casse le login » : le SDK est déjà fail-open sur `RedisError`/`OSError` (`REDIS_RATE_LIMIT_FAIL_CLOSED=false` par défaut), `_guard` élargit à *toute* exception. Le pont async→sync utilise `anyio.from_thread.run` : il exige un thread worker FastAPI, ce qu'est une route `def` synchrone comme `/auth/login` — passer cette route en `async def` ferait dégrader le limiteur en « pas de throttling » (un warning par appel), c'est documenté dans le module et couvert par un test. Le choix du backend au démarrage garde son ping de reachability (client sync jeté aussitôt : attendre un ping async à l'import lierait le pool à une boucle d'événements morte), donc un Redis injoignable au boot donne toujours le limiteur en mémoire. 12 tests (contre 10) : les cas Redis sont désormais joués *depuis un thread worker* via `anyio.to_thread.run_sync`, c'est-à-dire le vrai chemin d'appel de FastAPI, plus un test qui vérifie la dégradation hors thread worker, et surtout un test qui **fige la surface du SDK** (`hit`/`peek`/`reset`, leurs paramètres, les champs de `RateLimitResult`) : comme `_guard` avale tout, un renommage en 0.9 se lirait sinon comme « autorise la tentative » et désactiverait le throttling en silence. Dépendance épinglée `>=0.8.0,<0.9.0` pour la même raison. Vérifié : ruff, ruff format, ty et 249 tests serveur au vert.
- [x] 2026-08-15 — FIX: prek configuré par un unique `prek.toml` (tu avais déjà indexé la suppression de `.pre-commit-config.yaml` et `.prek.md`). Les 19 hooks sont repris à l'identique, avec deux principes qui remplacent les `rev` épinglés à la main. D'abord, tout ce que prek fournit nativement passe en `repo = "builtin"` : les 10 hooks qui venaient de `pre-commit-hooks` (espaces, EOF, YAML/JSON/TOML, gros fichiers, clés privées, conflits de casse/merge, fins de ligne) existent tous en Rust dans prek 0.4.13 (`prek util list-builtins`) — plus de clone, plus d'environnement à construire, plus de `rev` à bumper. Ensuite, ruff, ruff-format, ty et pytest passent par `uv run` et oxlint/oxfmt par `bun run` : la version utilisée par le hook est celle déjà épinglée dans `pyproject.toml` / `package.json`, donc un hook ne peut plus linter avec une version différente de la CI (c'était le cas : le repo ruff était figé à v0.13.0 alors que le projet exige ≥ 0.16.3). Restent deux dépôts distants, hadolint et pygrep-hooks, qui n'ont pas d'équivalent builtin. Un bug latent corrigé au passage : `check-json` échouait sur `apps/next/tsconfig.typecheck.json`, qui est du JSONC (ses commentaires expliquent justement pourquoi il diffère du tsconfig principal) — l'exclusion couvre désormais les `tsconfig*.json` comme elle couvrait déjà `.vscode/`. Le bloc `ci:` de l'ancien fichier n'est pas repris : il configurait pre-commit.ci, service que le projet n'utilise pas. `make setup` installe maintenant les shims (`uv run prek install`), ce que `.prek.md` prétendait déjà ; le README récupère la doc utile du fichier supprimé. Vérifié : `prek validate-config` OK, `prek list` montre les 19 hooks, et `prek run --all-files` passe sur les 17 exécutables ici (hadolint et pytest demandent Docker, inaccessible depuis le bac à sable). Deux fichiers corrigés par les hooks au passage : une espace en fin de ligne dans `.env.example` et dans `TODO.md`.
- [x] 2026-08-15 — FEAT: détail d'un dataset, ses sources et l'historique des analyses, plus l'ajout d'une source depuis ce détail. Côté serveur, un seul point d'entrée nouveau : `GET /dataset/{name}/sources` (`get_dataset_sources_view`), qui répond deux choses distinctes que rien n'exposait jusqu'ici. **`sources`** agrège les items actifs du dataset par `input.source_url` — c'est-à-dire ce qui a *réellement* produit des paires : une ligne par page crawlée, fichier uploadé ou compte GitHub, avec le nombre de paires, la première et la dernière apparition, et un `kind` (web/file/github/unknown) déduit du schéma de l'URL (les pipelines écrivent `file://<nom>` et `github://<user>`). **`history`** reprend les runs Langfuse (un run = une génération) et en sort la graine analysée, la version, les pages lues et les doublons écartés ; une stat absente reste `null` plutôt que de se lire « 0 ». Deux pièges traités : les dates Langfuse arrivent tantôt avec offset tantôt sans, et comparer les deux lève `TypeError` en plein `min`/`max` (`_as_utc` les normalise) ; et les runs sont best-effort côté sync (`sync_qa_to_langfuse` avale déjà leur échec), donc un run illisible vide l'historique sans casser la vue des sources — alors qu'un Langfuse injoignable, lui, remonte bien en 503. Côté front, la page `/datasets/[id]` était une page isolée sans lien entrant : les lignes de `/datasets` pointent maintenant dessus, et la page est reprise dans le shell de l'app (skin `.dd-page` du mockup) avec trois onglets — paires Q/A (paginées, inchangées), Sources (tableau, lien sortant sur les seules sources web) et Historique (frise des runs, le plus récent marqué « Latest »). L'ajout de source réutilise le pipeline existant plutôt que d'en dupliquer un : le CTA « Add a source » ouvre `/generate?dataset=<nom>`, la page de génération préremplit le nom et annonce que les paires seront ajoutées à ce dataset (générer sur un nom existant déduplique contre l'existant et enregistre la version suivante). Les caches react-query des sources sont invalidés après génération et après nettoyage. `DatasetDetail` (l'ancien composant de détail) est supprimé, remplacé par la page. 8 tests service + 4 tests API ajoutés (dont l'entrée du nouvel endpoint dans les deux tables paramétrées 503/500). Vérifié : ruff, ruff format, ty, oxlint, oxfmt, `bun run typecheck`, `bun run test` et 246 tests serveur au vert ; client TypeScript régénéré (`make api-client`, génération stable en deux passes). Les 4 tests API ajoutés n'ont pas pu être exécutés ici — ils dépendent du Postgres jetable (Docker inaccessible depuis le bac à sable), comme les 172 autres tests DB.
- [x] 2026-08-15 — CHORE: la CI Node exécute enfin le typecheck (`run-typecheck: true` dans `ci-cd.yml`) — jusqu'ici seul oxlint gardait le front, et oxlint ne regarde jamais les types. Le workflow partagé lance `bun run typecheck` : le script manquait, il est ajouté (`tsc --noEmit -p tsconfig.typecheck.json`). Ce tsconfig dédié est identique au principal **moins** `.next` : ces fichiers sont du build output, absents en CI (checkout neuf) mais présents en local, où un `.next` laissé par un `next dev` plus ancien fait échouer son validator périmé (`Type 'Route' does not satisfy the constraint 'never'`) — sans cette exclusion la même commande passerait en CI et échouerait sur la machine du dev, pour une raison étrangère à ses changements. Les types de routes/pages restent validés par `next build`, que la CI lance sur les PR. Vérifié : `bun run typecheck` sort à 0 sur le code source.
- [x] 2026-08-15 — DOCS: `docs/authentication.md` ajouté (l'auth n'était documentée que dans le README, alors que la CI publie `docs/` sur le site central). Couvre les deux cookies httpOnly et pourquoi l'access token est court, les deux rôles et la liste exacte des routes admin-only, la rotation des refresh tokens par famille et la détection de rejeu, le throttling du login et ses deux backends (dont la garantie fail-open), le flux OIDC et ses règles de résolution de compte (y compris pourquoi un email non vérifié ne sert jamais à lier un compte), le CORS crédentialé, le tableau complet des variables d'env, la référence des endpoints et une section dépannage. Pointeur ajouté depuis le README, comme pour `docs/crawling.md`.
- [x] 2026-08-15 — CHORE: `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead` résorbé. `httpx2` ajouté au groupe `dev` : seul `TestClient` en a besoin, le code applicatif (scraper, github, langfuse) continue d'utiliser le `httpx` des dépendances runtime — les deux s'installent côte à côte sous des noms de module différents. Les 410 tests passent, l'avertissement a disparu.
- [x] 2026-08-15 — TEST: `apps/server/api/collections.py` (58.97 %) et `api/dataset.py` (69.14 %) passent tous les deux à **100 %**. Les lignes manquantes étaient exclusivement les branches de mapping d'erreurs (`LangfuseUnavailableError -> 503`, `Exception -> 500`) plus, côté collections, l'endpoint `POST /collections/{name}/search` entièrement non testé. Deux tables paramétrées couvrent le 503 et le 500 de *chaque* endpoint d'un coup, donc un endpoint ajouté sans elles se verra comme une chute de couverture ; s'y ajoutent les cas propres à la recherche (succès, passage des paramètres de réglage au service, défauts du schéma, requête vide → 422, 503 Qdrant, 404 collection non synchronisée).
- [x] 2026-08-15 — TEST: `apps/server/api/debug.py` passe de 0 % à **100 %** (`tests/api/test_debug.py`, 21 tests). Le point sensible demandé est couvert : le routeur est monté *sans* le `Depends(get_current_user)` que portent tous les autres, donc seul `DEBUG_LOGS` sépare un déploiement d'un flux de logs bruts non authentifié — un test recharge le vrai `server.main` et vérifie l'absence/présence de `/debug/logs` selon le drapeau, et le parsing env→drapeau est testé séparément sur `Config` (dont `DEBUG_LOGS=` vide, que `make env` recopie). Deux pièges rencontrés, tous deux documentés dans le fichier : les routeurs inclus restent imbriqués (`_IncludedRouter`) dans cette version de FastAPI, donc scanner `app.routes[].path` déclarait tout absent et faisait passer l'assertion « non monté » quoi qu'il arrive — la vérité se lit dans le schéma OpenAPI ; et recharger `server.core.config` échangeait l'identité du singleton `config`, cassant un test de `test_generate.py` qui le patche — c'est l'*attribut* qui est patché désormais. Le stream lui-même (replay du backlog, keep-alive, livraison à chaud, désabonnement) est piloté via `response.body_iterator` : `TestClient.stream()` se bloque indéfiniment sur un générateur sans fin.
- [x] 2026-08-12 — FEAT: passage de SQLite à **PostgreSQL**, seul backend supporté. Service `postgres:17-alpine` ajouté au `docker-compose.yml` (volume `postgres_data`, healthcheck `pg_isready -U … -d …`, port hôte 5452 = lane slot 2 du projet) ; le `server` attend son healthcheck puisque les migrations Alembic tournent au démarrage. `core/database.py` refuse toute URL non-PostgreSQL et réécrit `postgres://` / `postgresql://` vers le driver `psycopg` (v3, seul installé), avec `pool_pre_ping` ; l'URL par défaut est dérivée des `POSTGRES_*` pour que `make dev-local` marche sans config. Migration `d1e2f3a4b5c6` réécrite : elle droppait une FK par un nom inventé pour le mode batch SQLite, qui n'existe pas sous Postgres (auto-nommée `qa_sources_page_snapshot_id_fkey`) — le nom est désormais réfléchi via l'inspecteur, donc la chaîne complète s'applique sur une base neuve. Tests migrés sur un vrai Postgres éphémère (testcontainers, `TEST_DATABASE_URL` pour réutiliser une instance existante) ; `make test` demande donc un daemon Docker.
- [x] 2026-08-09 — FIX(URGENT): **cause réelle du 500 au login.** `os.getenv` ne retombe sur son défaut que si la variable est *absente*, jamais si elle est présente mais vide — or `.env.example` livre `AUTH_REFRESH_COOKIE_NAME=` vide et `make env` le recopie tel quel. Résultat : `config.auth_refresh_cookie_name == ""` → `response.set_cookie(key="")` → `CookieError: Illegal key ''`. Signature caractéristique : **bons identifiants → 500, mauvais → 401** (le mauvais mot de passe n'atteint jamais l'émission du cookie), ce qui faisait passer le bug pour un problème d'identifiants. Reproduit à l'identique en local, puis vérifié corrigé (200 + `access_token`/`refresh_token`) y compris sur le conteneur. Correction générique : helper `_env()` dans `core/config.py` traitant vide/blanc comme absent, appliqué aux 40 lectures d'env — ça couvre aussi `CRAWL_MAX_DEPTH=` / `CRAWL_DELAY_SECONDS=` vides, qui faisaient planter `int("")`/`float("")` au démarrage. 6 tests de non-régression ajoutés (`tests/core/test_config.py`).
- [x] 2026-08-08 — FIX: latent, découvert en cherchant le 500 ci-dessus (ce n'en était **pas** la cause) : le repli « jamais casser le login » de `rate_limit.py` n'existait qu'au *choix* du backend (`_build_login_limiter`, au démarrage). Une fois `RedisSlidingWindowRateLimiter` retenu, un Redis tombant *ensuite* faisait remonter chaque `zcard`/`zadd`/`delete` dans la route, soit un 500 par tentative. Ajout d'un `_guard` qui dégrade en « pas de throttling » (fail-open, warning loggué). Test `test_redis_going_down_does_not_break_logins` ajouté.
- [x] 2026-08-08 — FIX: le seeding des comptes de dev était muet dans les deux cas qui font échouer le login. `seed_dev_users` sautait un compte existant sans rien logger (donc mot de passe ≠ défaut documenté, sans trace), et `SEED_DEV_USERS` non défini ne logguait rien du tout — seul symptôme : un 401 sur `/auth/login`. Log ajouté dans les deux branches (`services/users.py`, `main.py`). 30 tests auth/users passent.
- [x] 2026-08-08 — FIX: `make dev` échouait au build de l'image `next` (`bun install --frozen-lockfile`, exit 1) — `package.json` était passé à `typescript ^7.0.2` / `oxfmt ^0.62.0` alors que `bun.lock` pinnait encore `5.9.3` / `0.59.0`, et `--frozen-lockfile` échoue par construction quand les deux divergent. `bun.lock` régénéré (374 paquets) ; `bun install --frozen-lockfile` — la commande exacte du Dockerfile — repasse. Reformatage oxfmt 0.62 appliqué au passage sur 4 pages (`jobs`, `prompts`, `quality`, `sources`), purement du retour à la ligne.
- [x] 2026-08-08 — FIX: `apps/next/vitest.config.ts` excluait `['node_modules', '.next']`, motifs qui ne matchent que la racine — depuis que `make api-client` installe `apps/next/openapi-codegen/node_modules`, vitest ramassait les tests des dépendances et la suite échouait (3 fichiers en erreur). Remplacé par `['**/node_modules/**', '**/.next/**']` ; `bun run test` repasse au vert (1 fichier, 4 tests).
- [x] 2026-08-08 — CHORE(URGENT): `make api-client` relancé et `apps/next/api/` régénéré depuis le schéma dumpé par `api-schema` (30 paths). Le client couvre bien `quality-rules` et `QaStatsResponse` ; le seul écart restant avec `HEAD` est la ligne `baseUrl` de `types.gen.ts` (`http://localhost:8020` → `` `${string}://${string}` ``), déjà indexée. `make api-check` passe au vert. Reste à commiter (non commité ici).
- [x] 2026-08-07 — FIX: le job CI « API client drift » ne pouvait pas devenir vert — `dump_openapi.py` écrivait le schéma avec `sort_keys=True`, ce qui réordonnait les paths et faisait sortir `types.gen.ts` dans un ordre différent d'une génération contre le serveur live ; aucun `make api-client` n'aurait pu résorber ce diff. `sort_keys` retiré (ordre natif de l'app conservé) et `api-client` unifié sur le même schéma dumpé que `api-check` — un seul chemin de génération, plus besoin de serveur démarré. `apps/server/scripts/dump_openapi.py`, `Makefile`, README.
