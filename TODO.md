# TODO

## 🔴 En cours

## 🟡 À faire

## 🟢 Idées / backlog
- [ ] feat: ajouter le badget du nombre de collections générer avec qdrant sur le menu 'colelctions' de la side bar
- [ ] fix: server-fastapi  | WARNI [root] Skipping https://kevindb.dev/: Failed to crawl https://kevindb.dev/: {"detail":"URL blocked (SSRF protection): URL resolves to a blocked address"}

## 🤖 Claude — recommandations
- [ ] DOCS(auth): ajouter les variables `AUTH_*` et `OIDC_*` à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — documenté dans le README en attendant ; **inclure les nouvelles** `AUTH_REFRESH_TOKEN_TTL_SECONDS` et `AUTH_REFRESH_COOKIE_NAME` (+ `NEXT_PUBLIC_REFRESH_COOKIE_NAME` côté front si custom)
- [ ] DOCS(qdrant): ajouter `QDRANT_URL`, `QDRANT_API_KEY`, `QDRANT_COLLECTION_PREFIX` à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — sinon la page Collections reste en mode « non configuré »
- [ ] DOCS(crawl): ajouter `CRAWL_DELAY_SECONDS` et `CRAWL_MAX_PAGES_PER_DOMAIN` à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — sinon les nouveaux garde-fous de coût du crawl restent invisibles
- [ ] DOCS(auth): ajouter `REDIS_URL` (+ `REDIS_HOST_PORT`/`REDIS_IMAGE` pour compose) à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — sinon le rate-limiter login reste in-process en dev

## ✅ Fait
- [x] 2026-07-09 — DOCS(arch): nouvelle section « Langfuse is a hard dependency » dans le [README.md](README.md) (après « Crawling & Versioning ») documentant que Langfuse est la seule source de vérité pour les datasets — lectures (`GET /dataset`, `/langfuse/preview`, `/langfuse/export`) en 503 si indisponible, écritures (auto-sync) sans fallback local si sautées/échouées (les paires restent dans la réponse HTTP mais ne sont stockées nulle part côté serveur — vrai depuis le refactor `Dataset`/`QASource` de cette session), suppression qui laisse le shell vide du dataset (pas d'API delete-dataset côté Langfuse)
- [x] 2026-07-09 — FEAT(ui): `crawl_delay_seconds`/`max_pages_per_domain` exposés dans le formulaire /generate (`components/dataset/dataset-generate.tsx`), à côté de `max_depth`/`max_pages` déjà câblés — nouveaux champs dans la grille des contrôles crawl, passés à travers `GenerateParams`/`useGenerateDataset` (`hooks/use-datasets.ts`) jusqu'à `DatasetGenerationRequest`. **Effet de bord découvert en cours de route** : le SDK front (`apps/next/api/*.gen.ts`) était très en retard (routes auth/langfuse/collections/file/github absentes) ; régénéré via `bun run api:generate` contre un serveur local jetable (DB sqlite temporaire hors dépôt) — ce qui a révélé que la config `openapi-ts` n'avait pas `output.clean: false`, donc le premier run a **supprimé** `api/sdk.ts`/`api/types.ts` (wrappers écrits à la main, sensés survivre à la régénération) ainsi que le test vitest ajouté plus tôt cette session ; `sdk.ts`/`types.ts` restaurés via git (trackés), le test vitest recréé à l'identique (non tracké, perdu). `clean: false` ajouté à `openapi-ts.config.ts` pour que ça ne se reproduise plus. Vérifié en navigateur (login + remplissage des champs + toggle crawl) via preview, plus `tsc --noEmit`/`eslint`/`vitest run` — tous verts. Ferme aussi la recommandation historique « régénérer le SDK frontend »
- [x] 2026-07-09 — PERF(qdrant): `sync_dataset_to_qdrant` (`services/qdrant.py`) embed les Q/A par lots de `_EMBED_BATCH_SIZE` (100) via `_embed_in_batches` au lieu d'un unique appel `embeddings.create` pour tout le dataset — évite de dépasser la limite de tokens/taille de requête sur les gros datasets. L'ordre des vecteurs reste aligné avec les items. Test ajouté (`test_sync_embeds_in_batches`, `tests/services/test_qdrant.py`) vérifiant le découpage en lots (5 items / lot de 2 → 3 appels) et l'alignement item↔vecteur
- [x] 2026-07-09 — TEST(next): bootstrap de vitest pour `apps/next` (n'existait pas : aucun framework de test, aucune config, `run-test: false` en CI) — `vitest` en devDependency, script `"test": "vitest run"`, `vitest.config.ts` (env `node`). Test de l'intercepteur de refresh `api/sdk.ts` (`api/sdk.refresh.test.ts`, 4 cas : rejeu après refresh réussi, dédup `refreshInFlight` sur 401 concurrents, garde `NO_REFRESH_PATHS`, pas de rejeu si le refresh échoue) — piloté via le vrai pipeline `@hey-api/client-fetch` avec `fetch` mocké (`sdk.ts` n'exporte pas `client`, donc importé depuis `./client.gen` en parallèle pour partager le même singleton muté par les side-effects de `sdk.ts`). `run-test: true` activé dans `.github/workflows/ci-cd.yml` (job `ci-node`, `bun run test`). Vérifié en exécutant réellement `bun run test`/`lint`/`tsc --noEmit` (pas seulement écrit à l'aveugle)
- [x] 2026-07-09 — CHORE(auth): purge des `refresh_tokens` révoqués/expirés au démarrage. `purge_expired_refresh_tokens` (`services/auth.py`) fait un `DELETE WHERE revoked_at IS NOT NULL OR expires_at < now()` ; branché dans le `lifespan` de `main.py` (thread worker, comme le seeding des comptes dev), tourne à chaque démarrage du serveur — pas de cron séparé, volume trop faible pour le justifier. Tests ajoutés dans `tests/services/test_auth.py` (`TestPurgeExpiredRefreshTokens`)
- [x] 2026-07-09 — TEST: `LLMService.client`/`instructor_client` (`services/llm.py`) et `QAAgentService.client` (`services/agent.py`) sont passés en `functools.cached_property` au lieu d'être construits dans `__init__` — la construction du vrai client `openai.OpenAI()`/`AsyncOpenAI()` (qui lit le bundle certifi via SSL, refusé par le sandbox) n'a plus lieu tant que `.client` n'est pas réellement accédé, ce qui est le cas dans tous les tests concernés (méthodes mockées directement). **Toute la suite tourne maintenant en sandbox : 318/318 tests passent** (avant : 8 failed + 11 errors)
- [x] 2026-07-09 — DOCS: nouvelle section « File uploads (PDF/image) » dans le [README.md](README.md) documentant `OPENAI_VLM_MODEL` comme requis pour `/dataset/generate/file` (400 sans lui), avec mention du champ de formulaire `model_vlm` pour l'override par requête — suit le style de la table existante « Crawling & Versioning »
- [x] 2026-07-09 — PERF(github): `fetch_account_docs`/`GitHubService` (`services/github.py`) sont passés en async (`httpx.AsyncClient`) et minent les dépôts en parallèle via `asyncio.gather` (concurrence plafonnée à 5, `asyncio.Semaphore`) au lieu d'un repo après l'autre — l'ordre des chunks retournés reste déterministe (ordre de la liste des dépôts, pas l'ordre de complétion). `max_repos` était déjà exposé de bout en bout (schema/API/SDK/formulaire `/generate`), donc rien à faire côté UI. Tests réécrits en async (`tests/services/test_github.py`, + un test dédié à la concurrence/l'ordre)
- [x] 2026-07-09 — REFACTOR(arch): suppression complète des modèles `Dataset`/`QASource` et de la table locale associée. Dédup et auto-sync ont été migrés vers Langfuse : `QAService` (`services/qa.py`) est désormais scopé par dataset et interroge `get_dataset_items(dataset_name)` pour son pool de dédup en mémoire (au lieu d'un scan global `QASource` cross-dataset) ; `DatasetPipeline._sync_to_langfuse` (`pipelines/dataset.py`) pousse directement les paires générées dans cet appel plutôt que de relire toute la table locale (compromis assumé : plus de "self-healing" resync si un run précédent a été sauté — décision produit validée cette session). `Dataset`/`QASource` supprimés de `models/`, `services/dataset.py` supprimé (mort), routes `/dataset/generate*` n'ont plus de dépendance DB. Migration Alembic `e2f3a4b5c6d7` (drop `qa_sources`+`datasets`, upgrade/downgrade testés manuellement en round-trip). Tests réécrits : `tests/services/test_qa.py`, `tests/pipelines/test_dataset.py`, `tests/migrations/test_env.py` ; `tests/models/test_dataset.py` et `tests/services/test_dataset.py` supprimés (couvraient du code mort)
- [x] 2026-07-09 — REFACTOR(arch): `/langfuse/export` et `/langfuse/preview` lisent désormais Langfuse directement (`get_dataset_items`/`get_dataset_view`) au lieu de `Dataset`/`QASource` en DB locale — décision produit confirmée cette session, débloquant ce volet du refactor `Dataset`/`QASource`. Suppression au passage des fonctions mortes `get_datasets`/`get_dataset_by_id`/`analyze_dataset_similarities`/`clean_dataset_similarities` dans `services/dataset.py` (déjà remplacées par `services/dataset_reads.py` côté `/dataset`, plus aucun appelant en dehors de leurs propres tests). Tests réécrits en conséquence (`tests/api/test_langfuse.py`, `tests/services/test_dataset.py`)
