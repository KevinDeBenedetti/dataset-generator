# TODO

## 🔴 En cours

## 🟡 À faire
- [ ] REFACTOR(arch): supprimer à terme les modèles `Dataset`/`QASource` (+ migration Alembic de drop, pas suppression des fichiers de révision). `PageSnapshot`/`CleanedText` **sont déjà droppés** (voir ✅ Fait). **Reste** : dropper `Dataset`/`QASource` supposerait de faire lire `/langfuse/export` + `/langfuse/preview` ailleurs que la DB locale (ex. re-sync direct depuis Langfuse, déjà source de vérité pour les autres lectures) — décision produit **explicitement écartée** cette session au profit de garder `QASource` comme store d'export (voir historique ✅ Fait). Sans cette décision, `Dataset`/`QASource` restent nécessaires : `QASource` sert de store `/langfuse/export`/`/langfuse/preview` (bouton d'export UI live) et la dédup en mémoire ; `Dataset` reste conteneur FK tant que `QASource` existe. Bloqué sur une décision produit, pas sur du travail technique

## 🟢 Idées / backlog
- [ ] feat: ajouter le badget du nombre de collections générer avec qdrant sur le menu 'colelctions' de la side bar
- [ ] fix: server-fastapi  | WARNI [root] Skipping https://kevindb.dev/: Failed to crawl https://kevindb.dev/: {"detail":"URL blocked (SSRF protection): URL resolves to a blocked address"}

## 🤖 Claude — recommandations
- [ ] CHORE(next): régénérer le SDK frontend (`apps/next` → `npm run api:generate`) après la suppression de la route `/q_a/id/{qa_id}` **et** l'ajout de `POST /dataset/generate/file` et `POST /dataset/generate/github` — `getQaByIdQAIdQaIdGet` reste exporté (mort) et les nouveaux endpoints fichier/github ne sont pas encore dans le client généré (nécessite le serveur lancé pour dumper l'OpenAPI)
- [ ] DOCS: documenter `OPENAI_VLM_MODEL` comme **requis** pour la source fichier (le endpoint `/dataset/generate/file` renvoie 400 « No vision model configured » sans lui)
- [ ] PERF(github): `fetch_account_docs` est séquentiel (1 repo après l'autre, plusieurs appels API/repo) — pour un compte avec beaucoup de dépôts, paralléliser/limiter (et exposer `max_repos` dans l'UI) éviterait les temps longs et la pression rate-limit
- [ ] TEST: les tests qui instancient un vrai client OpenAI/httpx (ex. `DatasetPipeline`→`LLMService`→`openai.OpenAI()`) échouent en sandbox car `ssl.create_default_context` lit le bundle certifi `*.pem` (lecture refusée) — mocker le client à la construction dans ces tests (ou rendre `LLMService` lazy) pour faire tourner toute la suite en sandbox
- [ ] DOCS(auth): ajouter les variables `AUTH_*` et `OIDC_*` à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — documenté dans le README en attendant ; **inclure les nouvelles** `AUTH_REFRESH_TOKEN_TTL_SECONDS` et `AUTH_REFRESH_COOKIE_NAME` (+ `NEXT_PUBLIC_REFRESH_COOKIE_NAME` côté front si custom)
- [ ] CHORE(auth): purge périodique de la table `refresh_tokens` — les lignes expirées/révoquées ne sont jamais supprimées (croissance illimitée). Ajouter un `DELETE WHERE revoked_at IS NOT NULL OR expires_at < now()` (tâche startup ou cron) — non bloquant tant que le volume est faible
- [ ] TEST(next): l'intercepteur de refresh `api/sdk.ts` (dédup `refreshInFlight`, rejeu de la requête, garde `NO_REFRESH_PATHS`) n'est couvert par aucun test — non exécutable en sandbox (toolchain front dans Docker) ; ajouter un test vitest avec `fetch` mocké quand la CI front tourne
- [ ] DOCS(qdrant): ajouter `QDRANT_URL`, `QDRANT_API_KEY`, `QDRANT_COLLECTION_PREFIX` à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — sinon la page Collections reste en mode « non configuré »
- [ ] PERF(qdrant): `sync_dataset_to_qdrant` embed tous les Q/A en un seul appel `embeddings.create` → batcher (ex. 100/req) pour les gros datasets qui dépasseraient la limite de tokens/taille de requête
- [ ] DOCS(crawl): ajouter `CRAWL_DELAY_SECONDS` et `CRAWL_MAX_PAGES_PER_DOMAIN` à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — sinon les nouveaux garde-fous de coût du crawl restent invisibles
- [ ] FEAT(ui): exposer les contrôles de coût du crawl (`max_depth`, `max_pages`, `crawl_delay_seconds`, `max_pages_per_domain`) dans le formulaire /generate — l'API les accepte déjà mais l'UI ne les envoie pas
- [ ] DOCS(arch): documenter que Langfuse est désormais une **dépendance dure** des datasets (lecture/écriture/suppression renvoient 503 si Langfuse indisponible) — n'a plus de fallback local ; valable car Langfuse n'a pas d'API delete-dataset (le shell vide d'un dataset supprimé persiste)
- [ ] DOCS(auth): ajouter `REDIS_URL` (+ `REDIS_HOST_PORT`/`REDIS_IMAGE` pour compose) à `.env.example` (non modifiable depuis l'agent : bloqué par le sandbox) — sinon le rate-limiter login reste in-process en dev

## ✅ Fait
- [x] 2026-07-09 — REFACTOR(arch): **`PageSnapshot`/`CleanedText` droppés** (tables + modèles). Mortes depuis le passage du scraper en stateless (Étape 1) — plus aucun code ne les écrit ni ne les lit. Nouvelle migration `d1e2f3a4b5c6` (chaînée sur `c9d0e1f2a3b4`) : `DROP TABLE cleaned_text` + `DROP TABLE page_snapshots`, et retrait de la FK (non nommée à l'origine) sur `qa_sources.page_snapshot_id` via `batch_alter_table` + `naming_convention` explicite (requis : SQLite ne supporte pas `ALTER TABLE DROP CONSTRAINT`, la contrainte d'origine était anonyme). La colonne `page_snapshot_id` elle-même est **conservée** (toujours nullable, juste sans FK) plutôt que de la retirer de `QASource`/`QAService`/pipeline/tests — changement plus large hors scope de cette tranche. Supprimé : `models/scraper.py` (fichier entier), la relation `Dataset.page_snapshots`, les ré-exports morts dans `core/__init__.py` (rien n'importait `from server.core import PageSnapshot` etc.) et `scraper` retiré de l'import d'enregistrement `models/__init__.py`. Migration testée **upgrade + downgrade** sur une copie du vrai `datasets.db` (schéma + FK + index vérifiés dans les deux sens). 329 passed (inchangé), ruff+ty+format OK sur tout `apps/server`.
- [x] 2026-07-09 — REFACTOR(arch): **dédup QA réellement basculée en mémoire** (Étape 2c — la bascule que 2a avait reportée). Décision produit prise cette session : `QASource` **reste persisté** (sert de store à `/langfuse/export`/`/langfuse/preview`, un bouton d'export UI live découvert en cours de route — casser cette lecture était le vrai risque de "2a", pas la sémantique de dédup). `QAService` charge désormais `QASource` **une seule fois par run de pipeline** (`_load_existing_entries`, lazy) au lieu d'une fois par paire Q/R, puis compare en mémoire via `classify_duplicate` (module pur de 2a) ; les nouvelles entrées sont ajoutées au pool **après le commit** de chaque appel (pas pendant la boucle) — préserve exactement l'ancien comportement où `autoflush=False` (voir `core/database.py`) empêchait déjà la dédup intra-lot de se voir elle-même ; un premier essai qui ajoutait au pool en cours de boucle a été détecté et corrigé via un test existant (`test_process_qa_pairs_mixed_results`) qui s'est mis à échouer. `QASource.check_for_duplicates`/`is_duplicate_by_similarity` (les adaptateurs DB de 2a, plus aucun appelant) supprimés — logique désormais uniquement dans `QAService` + `services/dedup.py`. Aucune perte de dédup fuzzy inter-runs (contrairement à ce que 2a redoutait), juste la suppression du scan O(table) répété par paire. 2 nouveaux tests de comportement cross-call (`test_process_qa_pairs_dedups_across_calls_without_requery`, `_reuses_the_same_in_memory_pool`) + 3 tests obsolètes de `test_dataset.py` remplacés par 1 test `_to_entry`. 329 passed, ruff+ty+format OK.
- [x] 2026-07-08 — REFACTOR(arch): **lectures `QASource`→Langfuse centralisées** (Étape 2b, de-risking du drop des modèles `Dataset`/`QASource`/`PageSnapshot`). Les 3 sites qui faisaient chacun `db.query(QASource).filter(QASource.dataset_id == X).all()` (pipeline `_sync_to_langfuse`, `GET /langfuse/preview`, `POST /langfuse/export`) passent désormais par un unique `get_qa_records_for_dataset(db, dataset_id)` dans `services/dataset.py` — aucun changement de comportement, juste un seul point d'accès au lieu de trois. Idem pour le bloc dupliqué « lookup dataset par nom + 404 avec la liste des datasets disponibles » (`/preview` et `/export`) : extrait en `_get_dataset_or_404` local à `api/langfuse.py`. Objectif : le futur remplacement de la lecture `QASource` (a bis) devient un changement dans une seule fonction plutôt que trois. 2 nouveaux tests (`test_get_qa_records_for_dataset`, `_empty`) + 10 tests `test_langfuse.py` + 19 `test_dataset.py` toujours verts ; 329 passed (+2 vs précédent), ruff+ty+format OK. Pas de changement de sémantique de dédup ni de comportement API — seule la (a bis) restante est risquée (voir 🟡 À faire).
- [x] 2026-07-08 — REFACTOR(arch): **dédup QA extraite en module pur** (Étape 2a, de-risking du drop des modèles `Dataset`/`QASource`/`PageSnapshot`). Nouveau `services/dedup.py` (`QAEntry`, `classify_duplicate`, `find_similar`) : la logique de comparaison (hash exact + similarité question/contexte via `SequenceMatcher`, seuil `CONTEXT_SIMILARITY_FLOOR=0.95`) est désormais pure, sans DB, testable unitairement — sémantique **inchangée** (exact prioritaire sur similar, similar limité aux entrées de même `source_url`). `QASource.check_for_duplicates`/`is_duplicate_by_similarity` deviennent de fins adaptateurs DB (`db.query(cls).all()` → `QAEntry` → délégation au module pur) ; **la persistance n'a pas encore bougé** (`QASource` toujours écrit/lu en DB, `process_qa_pairs` inchangé) — c'est volontaire, voir option choisie ci-dessous. 12 nouveaux tests purs (`test_dedup.py`) + 7 `test_qa.py` + 12 `test_dataset.py` toujours verts (dont le test qui mocke `QASource.check_for_duplicates`) ; 327 passed (+12 vs baseline), ruff+ty+format OK. Bascule réelle en mémoire (retirer `db.query` du chemin chaud) **reportée** : casserait `/langfuse/preview`, `/langfuse/export` et `_sync_to_langfuse` (lisent tous `QASource` par `dataset_id`), changerait la sémantique de dédup (perte du fuzzy inter-runs) et n'est pas testable de bout en bout en sandbox (pas de client OpenAI) — voir 🟡 À faire.
- [x] 2026-07-08 — REFACTOR(arch): **scraper rendu stateless** — prérequis (Étape 1) du drop des modèles `Dataset`/`QASource`/`PageSnapshot`. `ScraperService` n'a plus de session DB ni de persistance : `scrape_url`/`crawl_site` renvoient désormais des `ScrapedPage` (dataclass `url`+`content`) en mémoire au lieu de `PageSnapshot`. Suppression de `__init__(db)`, `add_page_snapshot`, `_save_snapshot`, `save_cleaned_text` (les tables `page_snapshots`/`cleaned_text` ne sont plus écrites ; `CleanedText` n'était de toute façon jamais relu). `DatasetPipeline.process_url` consomme les `ScrapedPage`, ne nettoie plus vers la DB et passe `page_snapshot_id=None` (comme les sources fichier/GitHub, colonne déjà nullable). `Dataset`/`QASource` restent (conteneur + dédup — tranches suivantes). Tests réécrits : `test_scraper.py` (12, stateless) + `test_dataset.py` (mocks `save_cleaned_text` retirés) ; 315 passed, ruff+ty+format OK. ⚠️ 9 failed + 10 errors préexistants (bug sandbox certifi/SSL, cf. reco TEST) — inchangés, non liés à ce diff (vérifié : `PermissionError` sur `ssl.create_default_context`).
- [x] 2026-07-08 — FEAT(auth): refresh token + rotation. Access token JWT ramené à **15 min** (`AUTH_TOKEN_TTL_SECONDS` 86400→900) + refresh token httpOnly long-vécu (**14 j**, `AUTH_REFRESH_TOKEN_TTL_SECONDS`). Backend : nouveau modèle `RefreshToken` (opaque, **stocké haché** SHA-256, notion de *famille*) + migration `c9d0e1f2a3b4` (upgrade/downgrade vérifiés sur SQLite) ; `services/auth.py` (`issue`/`rotate`/`revoke_refresh_token`) — rotation **usage unique** (chaque `POST /auth/refresh` révoque le token présenté et en émet un successeur dans la même famille), **détection de rejeu** (réutiliser un token déjà tourné révoque toute la famille → déconnexion de tous les appareils). `login`/OIDC posent les 2 cookies ; `logout` révoque la famille + purge les 2 cookies. Frontend : intercepteur de réponse dans `api/sdk.ts` qui, sur le **1er 401**, appelle `/auth/refresh` (dédupliqué via un `refreshInFlight` partagé pour éviter que des 401 concurrents ne se révoquent mutuellement) puis rejoue la requête ; `middleware.ts` considère la présence du cookie refresh comme session renouvelable. 45 tests auth OK (16 API + 20 service + oidc/route-protection), ruff+ty OK, README documenté. ⚠️ Toolchain frontend non exécutable en sandbox (`node_modules` hôte absent, dev via Docker) — intercepteur vérifié par revue contre le client généré ; à valider via `make dev` / `next build`.
- [x] 2026-07-08 — FEAT: documentation dynamique dans docs/ conforme au workflow réutilisable `cd-docs.yml` (github-workflows) et au site central kevindebenedetti.github.io. (1) `docs/crawling.md` : les 6 liens relatifs sortant de `docs/` (../apps/…, ../docker-compose.yml) remplacés par des URLs GitHub absolues — ils faisaient échouer le job `verify` (`check-docs-links`). (2) `docs/README.md` (simple TOC) remplacé par `docs/getting-started.md` (setup, services, 3 sources de génération, commandes make) — le site central génère lui-même l'index projet via `generate-config.ts`, comme dotfiles/tools/notes. (3) Nouveau job `dispatch-docs` dans `ci-cd.yml` (push sur main, après ci-python/ci-node) appelant `cd-docs.yml@v0.12.0` (même SHA épinglé que les autres jobs). Vérifié : scripts `check-docs-links` + `check-vitepress-md` passent en local, actionlint OK. ⚠️ Requiert la variable repo `DOCS_APP_CLIENT_ID` + le secret `DOCS_APP_PRIVATE_KEY` (non vérifiables/configurables depuis le sandbox).
- [x] 2026-06-30 — FIX(dashboard): données incorrectes (comptes Q/A + langue). (1) `qa_sources_count` venait de la métadonnée Langfuse `total_items` (périmée/absente) → ex. `python` affichait `null` au lieu de **323** ; `list_datasets_view` calcule désormais le compte réel via un nouveau `count_dataset_items` (1 requête `limit=1` → `meta.totalItems`), avec repli sur la métadonnée si l'appel échoue. (2) `target_language` n'était jamais écrit dans Langfuse → toujours `null` (breakdown « unknown ») ; `sync_qa_to_langfuse` le stampe maintenant dans la métadonnée du dataset (threadé depuis les 3 pipelines via `_sync_to_langfuse`). Correctif **forward-only** : les datasets existants restent `unknown` tant qu'ils ne sont pas régénérés/re-synced (langue non récupérable a posteriori). Vérifié en live (python → 323) ; 304 tests OK.
- [x] 2026-06-29 — FEAT(ui): /generate multi-sources — sélecteur de source (URL / fichier / GitHub) dans le formulaire, câblé sur les 3 backends ; **clôt la feature multi-sources** (URL + fichier + GitHub de bout en bout). Ajout des wrappers SDK `generateDatasetFromFile` (multipart) + `generateDatasetFromGitHub`, extension du hook `useGenerateDataset` (union discriminée par `source`), inputs conditionnels (upload fichier ; username/token GitHub + `max_repos`) ; le crawl reste réservé à la source URL. ⚠️ Toolchain frontend non exécutable en sandbox (pas de `node_modules` hôte, dev via Docker) — typecheck par revue uniquement ; à vérifier via `make dev` / `npm run lint` / `next build`.
- [x] 2026-06-29 — FEAT: /generate source **GitHub** (dépôts publics) — nouvel endpoint `POST /dataset/generate/github` + `DatasetPipeline.process_github` : récupère via l'API GitHub (token optionnel, **rate-limit only**, public uniquement) le README + docs top-level de chaque dépôt public, puis clean→QA→dédup→Langfuse. Ajout de `services/github.py` (`GitHubService` + `fetch_account_docs`) + schéma `GitHubGenerationRequest` + helper `_resolve_models` réutilisé. `QASource` sauvés en `github://<user>`, sans `page_snapshot`. 8 nouveaux tests (service mock httpx + endpoint + pipeline), suite 303 passed. Scope convenu : READMEs + docs top-level. Reste de la feature : UI frontend (voir 🟡 À faire).
- [x] 2026-06-29 — FEAT: /generate source **fichier (PDF/image)** — nouvel endpoint `POST /dataset/generate/file` (multipart) + `DatasetPipeline.process_file` : rasterise chaque page (pymupdf) puis transcrit via le VLM configuré (`OPENAI_VLM_MODEL`), réutilise le pipeline clean→QA→dédup→Langfuse. Ajout de `services/files.py` (`file_to_page_images`) + `LLMService.extract_text_from_image`. Les `QASource` sont sauvés en `file://<nom>` sans `page_snapshot`. 19 nouveaux tests (files/llm/pipeline/endpoint), suite +17 (295 passed) ; `pymupdf` ajouté aux deps. Tranche convenue avec l'utilisateur (source GitHub + UI restent — voir 🟡 À faire).
- [x] 2026-06-29 — FIX(auth): mots de passe dev par défaut faibles — nouveau flag `ENVIRONMENT` (défaut `production`, fail-closed) + `config.is_development` ; `seed_dev_users` exige désormais `DEV_ADMIN_PASSWORD`/`DEV_USER_PASSWORD` hors dev et refuse (`RuntimeError`) de seeder les défauts faibles `admin1234`/`user1234`. Un `SEED_DEV_USERS=true` fuité ne peut plus créer de comptes à identifiants connus. README + script de seed + tests mis à jour (14 tests users OK).
- [x] 2026-06-29 — REFACTOR(arch): supprimer le code mort autour des `QASource` (scope révisé — la prémisse d'origine « `QASource` plus lus » était fausse : lus dans `services/dataset.py`, `api/langfuse.py`, dédup pipeline). Supprimé uniquement le vrai code mort : route `/q_a/id/{qa_id}` + schéma `QAResponse`, et les 4 méthodes CRUD inutilisées de `QAService` (`add/delete/update/get_qa_source`) + leurs tests. Réécriture dédup-en-mémoire / sync-direct-Langfuse **non faite** (risquée, non testable en sandbox, perte de données). 12 tests q_a/qa OK, app intacte.
